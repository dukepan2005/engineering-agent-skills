interface:
  display_name: "Implementation Preread"
  short_description: "Read a ticket and code before implementation"
  default_prompt: "Use $implementation-preread to prepare a read-only implementation brief for AB#169."
policy:
  allow_implicit_invocation: false
