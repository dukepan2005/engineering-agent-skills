interface:
  display_name: "Task Model Planner"
  short_description: "Choose an execution profile for each task"
  default_prompt: "Use $task-model-planner to recommend execution profiles for every Task under AB#168."
policy:
  allow_implicit_invocation: false
