interface:
  display_name: "Task Model Planner"
  short_description: "Choose a profile for each Azure work item"
  default_prompt: "Use $task-model-planner to recommend execution profiles for every implementation-ready work item under AB#168."
policy:
  allow_implicit_invocation: false
