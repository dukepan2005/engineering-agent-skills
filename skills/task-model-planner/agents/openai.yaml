interface:
  display_name: "Task Model Planner"
  short_description: "Choose models and thinking levels for each task"
  default_prompt: "Use $task-model-planner to recommend implementation models and thinking levels for every task under AB#168."
policy:
  allow_implicit_invocation: false
